[project]
name = "day-1"
version = "0.1.0"
description = "Day one projects"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "black>=26.5.1",
    "flake8>=7.3.0",
    "isort>=8.0.1",
]
